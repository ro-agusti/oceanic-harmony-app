// import express from "express";
// import { verifyToken, verifyAdmin } from '../helpers/middleware/authMiddleware.js'; // Importar el middleware
// import { createResponseType, updateResponseType } from "../controllers/responseController.js";

// const router = express.Router();

// router.post("/response-type", verifyToken, verifyAdmin, createResponseType);
// router.put("/response-type/:id", verifyToken, verifyAdmin, updateResponseType);

// export default router;